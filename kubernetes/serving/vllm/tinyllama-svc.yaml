apiVersion: v1
kind: Service
metadata:
  name: vllm-tinyllama
  namespace: llm-serving
  labels:
    app.kubernetes.io/name: vllm-tinyllama
    app.kubernetes.io/part-of: llm-serving
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vllm-tinyllama
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP